
public class Main {
    public static void main(String[] args) throws InterruptedException {
        // Object of a class that has both produce()
        // and consume() methods
        GUI gui = new GUI();
    }
    // This class has a list, producer (adds items to list
    // and consumer (removes items).
}

// n=1000000 buffersize=8 outputfile=test.txt
// n=999999 buffersize=8 outputfile=test.txt
// n=596374 buffersize=8 outputfile=test.txt
// n=159753 buffersize=8 outputfile=test.txt
// n=357951 buffersize=8 outputfile=test.txt
